/*用于Angular app（）和单元测试（）的TypeScript编译器配置。tsconfig.app.jsontsconfig.spec.json*/
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "baseUrl": "./",
    "module": "es2015",
    "types": []
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
